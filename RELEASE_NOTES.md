# DataDjinn v0.2.17

## Highlights

- 表结构编辑支持修改表名、字段名以及表和字段注释
- 连接树分组与连接排序、置顶和展开体验得到统一优化
- 大量表的加载先展示列表，表大小在后台逐步计算，避免阻塞界面
- 修复单列表格布局异常，保证行号、列头和内容区域宽度一致

## What's Changed

### 表结构与元数据

- 保存表结构修改后保持当前表节点状态，不再自动关闭表列表
- 支持修改表注释和字段注释，并修复达梦字段注释保存后不生效的问题
- 增加对数据库连接失效、驱动重连和连接池探活配置的兼容处理

### 连接树

- 分组统一显示在连接之前，分组内和连接内分别支持独立排序
- 顶级连接和分组支持置顶、取消置顶
- 分组支持双击展开和关闭，子节点缩进与层级保持一致
- 所有树节点加载时显示动态加载状态
- 大量表展开时先返回表列表，再异步计算并填充表占用大小，避免长时间阻塞或拖垮后端

### 表格布局与稳定性

- 修复只有一列数据时行号列异常变宽、列内容区域显示不完整的问题
- 修复长内容失焦后截断显示不带省略号的问题
- 保持多选、草稿标记和滚动恢复时的单元格边框与布局稳定

## Notes

- 当前版本号：`v0.2.17`
- 支持从 `v0.2.16` 直接升级

# DataDjinn v0.1.14

## Highlights

- 新增高斯数据库支持，可通过驱动管理配置 JDBC jar 并完成连接、浏览、预览和 SQL 执行
- 优化左侧连接树与资源树性能，补充分组、排序和对象分类列表查看能力，明显降低展开卡顿和分段感
- 增强 AI 数据查询、自动重连和异常兜底体验，同时修复达梦驱动切换等场景下的白屏问题

## What's Changed

- 新增高斯数据库支持：可在驱动管理中配置高斯 JDBC 驱动，并在新建连接中直接选择高斯数据库类型
- 左侧连接树支持连接分组、组内外排序和分类节点右键“查看列表”，可直接查看当前库下表/视图名称与注释
- 资源树展开、收起与滚动性能继续优化，并修复异常断连后节点展开状态残留的问题
- 表格预览区域补强单元格、行、列选择交互，优化分页切换与加载态反馈
- AI 查询数据时改为默认读取部分数据，仅在用户明确要求全量时再查全部；同时执行轮次上限从 12 提升到 24
- AI 与人工操作数据库时均支持连接关闭后的自动重连，降低长时间空闲后的执行失败概率
- 驱动管理改为“数据库类型导航 + 当前类型配置”的布局，便于扩展达梦、高斯等手动驱动数据库
- 优化 PostgreSQL / 高斯连接失败提示，并增加前端错误边界，避免运行时异常直接导致整页白屏
- 增强达梦驱动选择与历史字段回填逻辑，兼容旧连接数据并避免编辑连接或切换驱动时的异常渲染

## Notes

- 当前版本号为 `v0.1.14`
- 本次发布包含前端与后端联动改动，发布前已完成前端 typecheck、前端构建和后端语法校验

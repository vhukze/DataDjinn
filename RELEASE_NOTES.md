# DataDjinn v0.2.13

## Highlights

- 新建连接可直接选择或创建分组，右键可复制完整连接信息和 JDBC URL
- 重构 AI 上下文管理，清晰区分主上下文、临时主上下文和手动补充上下文
- 修复各类 SQL 数据库的行注释执行问题，并优化 ClickHouse 简单查询性能
- Windows 安装包显著瘦身，同时继续保持数据库能力完全离线可用

## What's Changed

### 连接管理

- 新建数据库连接时可选择已有分组，或在保存连接弹框中直接创建新分组，保存后自动归入对应位置
- 连接右键菜单新增“复制连接信息”，包含主机、端口、用户名、密码和当前数据库，便于在可信环境中快速复用
- 支持按 MySQL、PostgreSQL、高斯、Oracle、达梦、SQLite 和 ClickHouse 等数据库类型复制可直接使用的 JDBC URL

### AI 上下文

- 自动主上下文随当前打开的数据库或模式切换；手动添加的数据库和模式作为补充上下文持续保留
- 没有自动主上下文时，首个手动上下文会成为临时主上下文，可直接用于结构读取和数据库工具执行
- 上下文统一去重并显示总数，超过 3 个时默认折叠，可手动展开或收起
- PostgreSQL、高斯等层级数据库支持主上下文在数据库和模式之间向上合并、下钻或同库模式切换，不再重复保留父子上下文

### SQL 与 ClickHouse

- 修复格式化多行 SQL 包含 `--` 行注释、分号后带尾注释时可能执行失败的问题
- 只有注释、没有 SQL 的区域不再显示行首执行按钮，也不会被识别为可执行语句
- ClickHouse 查询移除额外的连接预检、`USE` 切库和超时设置 SQL，减少高延迟环境中的多次网络往返

### 安装与分发

- 收紧 Electron 运行时文件范围并启用最高压缩，移除测试缓存、开发依赖和构建中间产物
- 安装包瘦身后仍完整内置 Python 后端及 SQLite、MySQL、PostgreSQL、Oracle、MongoDB、Redis、ClickHouse、达梦和高斯相关离线能力
- GitHub Release 同时提供 Windows 安装包和绿色解压版，并在发布前启动打包产物验证离线后端

## Notes

- 当前版本号：`v0.2.13`
- 支持从 `v0.2.12` 直接升级

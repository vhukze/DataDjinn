# DataDjinn v0.3.11

## What's Changed

### 数据库扩展与连接体验

- 新增 Elasticsearch 连接支持，兼容 Elasticsearch 7.10+、8.x，并识别 OpenSearch 服务端。
- ClickHouse、Elasticsearch、Oracle 等体积较大的数据库能力改为按需安装的独立扩展，连接编辑器会提示扩展安装状态。
- 扩展更新支持稳定安装目录、占用中的 MCP 延迟替换和重启调用方提示。
- 改善达梦、JDBC 和 Elasticsearch 的连接超时、认证参数与服务端兼容处理。
- 保存的连接树分组、排序和显示库偏好使用版本化持久化，避免覆盖安装后回到根目录或默认选择。

### 工作区与数据浏览

- 优化大字段表格的滚动、选择、编辑和记录视图性能，减少列多时的卡顿和刷新闪烁。
- 左侧连接树搜索支持当前已加载节点的连续输入、匹配计数、上下切换和路径定位，输入过程中保持焦点。
- 树节点展开、折叠和数据库加载不再重置滚动位置。
- 左右侧栏拖动改为预览线，松开后再应用布局，降低拖动过程中的界面卡顿。
- 多条 SQL 执行失败时显示具体错误和红色失败状态。

### Git 版本管理与兼容性

- 表预览保存数据时只压缩并提交发生变化的表，结构和数据提交均在后台执行并显示进度。
- 表历史支持提交概要、变更 SQL、逐行逐字段数据差异，以及历史结构或数据恢复。
- MCP 加载连接配置时跳过暂不支持的数据库类型，不再因单条未知连接阻塞整个 MCP 服务。
- 修复主题状态被同步配置覆盖、Elasticsearch 认证表单与索引浏览相关问题。

## Notes

- Git 表数据版本管理仍处于试验阶段，建议先在测试数据验证后再用于生产数据回退或审计。
- 当前版本号：`v0.3.11`
- 支持从 `v0.3.10` 直接升级。

**Full Changelog**: https://github.com/vhukze/DataDjinn/compare/v0.3.10...v0.3.11

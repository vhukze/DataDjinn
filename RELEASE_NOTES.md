# DataDjinn v0.3.0

## Highlights

- 新增 GitHub 加密同步与连接级 Git 结构版本管理，支持跨设备合并和 DDL 历史查看。
- 新增按需安装的扩展体系，AI、MCP、JDBC/Java 和 Git 表数据版本管理不再随基础功能默认加载。
- 新增表数据快照和行级差异审查，完善数据库工作区、连接树与 SQL 执行体验。

## What's Changed

### GitHub 同步与版本管理

- 可在应用内打开浏览器完成 GitHub 授权，使用独立同步口令加密同步连接、密码、AI 配置和应用偏好到自动创建的私有仓库。
- 支持手动同步、每 15 分钟自动同步、三方合并和树形连接排序冲突对比；不同设备独立新增的连接和分组会保留原父级归属。
- 关系型连接可按数据库或模式选择纳管范围，自动或手动创建结构快照，查看独立提交历史与任一版本完整 DDL。
- 可选“Git 表数据版本管理”扩展支持不超过 5000 行的小表快照、自动后续快照和基于主键或单列唯一键的行级差异审查。

### 可选模块与运行时

- AI 助手、本机 MCP 服务、JDBC 数据库支持和 Git 表数据版本管理改为按需下载安装，安装过程提供下载、校验和解压状态。
- JDBC 模块会自动检测可用的 64 位 Java 8+；未检测到时自动安装 Java 17。
- MCP 服务默认关闭、仅限本机 STDIO；可限制连接访问范围，并将写操作作为独立授权开关。

### 数据库与工作区体验

- 强化连接树分组、排序、置顶、加载反馈和大对象列表加载体验。
- 优化表格预览和查询结果的选择、编辑、草稿标记、滚动恢复、列宽及少列布局一致性。
- SQL 编辑器完善多语句逐条执行、存储过程调用、关键字与过程补全，以及结果和错误信息展示。
- 完善连接空闲断开后的静默重连重试，以及达梦 JDBC、表结构编辑和注释处理兼容性。

## Notes

- 当前版本号：`v0.3.0`
- 表数据版本管理当前支持快照、提交历史和行级差异，不提供表数据回退或大表外部存储。
- 支持从 `v0.2.17` 直接升级。

**Full Changelog**: https://github.com/vhukze/DataDjinn/compare/v0.2.17...v0.3.0

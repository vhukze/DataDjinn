# DataDjinn v0.2.10

## Highlights

- 修复连接导入时同名分组被错误重命名为 `（1）` 的问题，现在会直接合并到已有同名分组
- 补充对应的自动化回归测试，覆盖 DBeaver 导入到已有同名分组的场景

## What's Changed

- 调整连接导入后的分组落库逻辑：同名分组复用现有分组 `id`，不再创建重复分组
- 导入连接会正确并入已有分组的连接顺序，不会打乱当前分组结构
- 新增 DBeaver 导入回归用例，验证不会生成 `分组名（1）`，并确保连接真正合并进原分组

## Notes

- 当前版本号：`v0.2.10`
- 当前 GitHub Release 工作流仅构建并发布 Windows 安装包

# DataDjinn v0.2.7

## Highlights

- 新增 SSH 隧道配置，支持在新建和编辑连接时直接走 SSH 网关访问数据库
- 连接编辑弹框改为横向布局，并补上 SSH 配置回显与单独测试 SSH 连接能力
- README 与发布流程说明同步收敛到 Windows 安装包交付

## What's Changed

- 新增连接级 SSH 隧道参数，支持 SSH 主机、端口、用户名、密码认证、私钥认证和私钥口令
- 后端接入 `sshtunnel`，为关系型数据库连接增加 SSH 隧道建立、复用、释放和连通性测试接口
- 新建/编辑连接弹框改为横向布局，优化窄宽度场景下的头部占位，并修复重复打开编辑弹框后表单字段丢失
- 补充 SSH 隧道、连接编辑回显和通用 smoke 基线回归用例，发版前通用功能基线已重新跑通

## Notes

- 当前版本号：`v0.2.7`
- 当前 GitHub Release 工作流仅构建并发布 Windows 安装包

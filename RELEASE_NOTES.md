# DataDjinn v0.2.14

## Highlights

- 查询结果、表预览和连接树支持更灵活的数据导出，可选择列、范围与格式
- 存储过程支持从右键菜单直接执行，并提供参数输入和结果展示
- 优化查询分页、断线重连与表格交互，导出和 DDL 弹框统一为玻璃视觉

## What's Changed

### 数据导出

- 查询结果和表预览支持导出 CSV、JSON、Markdown；表相关入口额外支持 SQL
- 导出时可选择列；查询结果可选择当前页或保留过滤、排序条件的全部数据
- 表导出 SQL 始终保留完整表结构，列选择仅影响 INSERT 数据；PostgreSQL 和高斯范围导出覆盖数据库中的全部业务模式

### 存储过程

- 存储过程右键菜单新增执行入口，支持 IN、OUT、INOUT、NULL 与默认参数输入
- 执行结果或输出参数在查询结果区域展示
- 存储过程和函数 DDL 自动补齐末尾分号，复制后可直接在命令行执行

### 查询与体验

- 修复查询结果存在后续页时末页按钮不可点击的问题，并明确展示分页按钮的禁用状态
- 连接闲置后执行预览、查看 DDL 等操作会自动无感重连
- 优化表格操作图标、单元格多选交互，以及导出和 DDL 弹框的统一玻璃视觉

## Notes

- 当前版本号：`v0.2.14`
- 支持从 `v0.2.13` 直接升级

# DataDjinn v0.2.16

## Highlights

- 表预览和查询结果升级为更接近 Excel 的多单元格选择与批量编辑体验
- 连接建立可随时停止；空闲断开后的数据库操作会静默重连并重试
- 连接树搜索支持匹配计数、上下切换和更清晰的当前项定位

## What's Changed

### 表格编辑与选择

- 支持 Ctrl 多选、Shift 矩形范围选择、拖拽选择，以及行号和列名选择
- 选中多个单元格后直接输入可批量编辑；Backspace 和 Delete 支持按正常编辑规则删除，未进入编辑时按 Delete 可批量清空
- 查询结果会为可安全定位到原始记录的字段提供同样的编辑能力；无法可靠定位的表达式或聚合结果不会提交修改
- 未提交的修改以橙色单元格和行号侧边标记显示，虚拟滚动后仍能稳定恢复选择状态
- 修复长文本单元格编辑失焦后丢失省略号、再次选中时内容溢出的问题

### 连接与搜索体验

- 双击打开连接期间可右键“停止连接”，已取消的迟到结果不会重新打开连接
- 连接、表预览和查询操作遇到断开、失效或重置的连接时，会自动静默重连并重试一次
- SSH 测试增加网关与整体请求超时；关闭连接弹框会立即重置测试中的加载状态
- 连接树搜索按连接名称和地址匹配，显示匹配数量并可在上一个/下一个结果间切换

## Notes

- 当前版本号：`v0.2.16`
- 支持从 `v0.2.15` 直接升级

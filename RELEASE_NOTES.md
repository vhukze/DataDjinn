# DataDjinn v0.2.11

## Highlights

- SQL 编辑器补全聚焦关键字、当前库表和字段，并支持逐条 SQL 直接执行
- SQL 超时时间可在设置中配置，默认 15 分钟
- AI 工具调用、表格选择、滚动条拖动和启动重启流程更稳定

## What's Changed

- 优化 SQL 单条语句识别和高亮；每条语句首行提供直接执行入口，移除建表模板类干扰提示
- 恢复表预览和查询结果中 WHERE 输入框的字段提示
- 修复停止 AI 任务时显示 `AbortError`，以及 AI 工具调用空工具名导致失败的问题
- 优化表格连续空单元格选区、横向滚动条拖动、行列单元格选择和虚拟表格重绘后的选中状态恢复
- 驱动管理修改 Java 设置时仅重启后端；Windows 下完整清理后端进程树，避免启动页卡住和残留进程
- 修复多个深色模式、设置弹框、导入导出和启动页的视觉问题
- 强化本地 IPC/API 访问校验并更新安全相关依赖

## Notes

- 当前版本号：`v0.2.11`
- GitHub Release 将自动构建并发布 Windows 安装包和绿色版

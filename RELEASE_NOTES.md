# DataDjinn v0.1.29

## Highlights

- 新增 DataGrip 连接导入、历史查询恢复和连接密码补录流程，连接管理更顺手
- AI 面板新增流式思考展示、模型切换、上下文容量进度条和自动压缩
- 查询与预览工作区继续增强，补齐快捷键、结果折叠、全表排序、Redis Key LIKE 浏览和多处交互优化

## What's Changed

- 新增 DataGrip 数据源导入，支持批量解析导入、重名自动追加 `(1)` `(2)`，并支持 ClickHouse 多端口连接导入
- 新增历史查询窗口，查询工作页默认自动保存，可按连接分组查看、重新打开和删除
- 打开连接时如果缺少密码或密码失效，会提示重新输入密码，连接成功后自动回写保存
- AI 面板支持回复与思考过程流式输出，思考内容可折叠查看，并新增模型切换、上下文进度条和自动压缩上下文
- 设置中新增 SQL 编辑器和 AI 窗口快捷键配置，快捷键仅在应用内生效
- SQL 编辑器支持执行当前语句或选中内容，并支持一次执行多条非查询 SQL
- 查询结果区默认按需显示，支持展开 / 收起，并能区分展示查询结果、执行成功信息和失败原因
- 表格预览与查询结果支持全表排序、值视图 JSON 格式化，以及更完整的单元格选择、复制、粘贴和右键操作
- Redis 浏览页新增 `KEY LIKE` 模糊查询、分页联动、行内编辑和统一提交，并统一了工具栏与输入区样式
- 优化左侧连接树、表格搜索、分栏拖动和表格交互性能，减少卡顿和延迟
- 修复 PostgreSQL 触发器 DDL 展示不完整的问题，补齐触发器函数体内容
- 修复 AI 生成 SQL 时误直接执行的问题，默认生成 SQL 会写入查询窗口，只有用户明确要求执行时才会落库

## Notes

- 当前版本号：`v0.1.29`
- 本次版本重点是连接导入、AI 对话体验、查询工作区和 Redis 浏览能力增强

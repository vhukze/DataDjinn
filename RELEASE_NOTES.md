# DataDjinn v0.1.12

## Highlights

- 优化左侧资源树在多连接、多节点场景下的展开、收起与滚动性能
- 修复左侧树虚拟渲染时的分段感，统一节点高度并减少展开/收起时的视觉断续
- 抽象手动驱动型数据库连接字段，达梦优先走通用驱动配置，为后续同类数据库接入复用打基础

## What's Changed

- 左侧资源树启用并校准虚拟列表高度，减少大节点量下的卡顿与重排抖动
- 连接节点标题改为单行紧凑布局，避免展开/收起时出现分段显示、分段消失
- 修复资源树和结果表区域的滚动体验问题，避免滚轮偶发失效
- 连接表单与后端连接存储新增通用 `driver_id` / `driver_path` 字段
- 达梦连接回显、保存与驱动选择逻辑兼容旧字段并优先使用通用驱动字段
- 新建连接菜单将需要手动配置驱动的数据库类型归到分割线下，便于后续扩展国产数据库

## Notes

- 本次版本重点是性能与连接抽象优化，不影响现有数据库连接和使用方式

# DataDjinn v0.3.10

## What's Changed

### Git 表数据版本管理（试验性）

- 表预览保存数据后改为只压缩和提交当前变化表，不再重新上传整个纳管范围；结构变更仍在后台创建库级快照。
- 表历史支持查看提交概要、变更 SQL 和逐行逐字段数据差异；恢复历史数据后会立即刷新当前表预览，并在后台记录新的提交。
- Git 提交说明改为变更概要，完整 SQL 单独保存；较大的压缩快照可通过 Git Blob API 正确读取。
- 从连接打开 Git 历史不再强制打开数据库连接；有后台任务时会在“新建查询”左侧显示任务入口和进度。

### 稳定性与 SQL 工作区

- 修复连接树在展开或折叠连接、库、模式、对象分组、表列等节点时偶发回到顶部的问题。
- 多条 SQL 独立执行时，每条失败语句会显示具体数据库错误，并使用红色失败状态；后续语句仍会继续执行和显示结果。

## Notes

- Git 表数据版本管理仍处于试验阶段，建议先在测试数据验证后再用于生产数据回退或审计。
- 当前版本号：`v0.3.10`
- 支持从 `v0.3.9` 直接升级。

**Full Changelog**: https://github.com/vhukze/DataDjinn/compare/v0.3.9...v0.3.10

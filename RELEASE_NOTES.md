# DataDjinn v0.2.15

## Highlights

- AI 写操作现在会严格继承当前连接、数据库和模式，跨库层级数据库不再误在默认库执行
- 高斯数据库完善建库、删库和 SQL 执行的自动提交兼容，并优化 JDBC 断线后的无感恢复
- 查询结果和表预览恢复稳定的默认列宽，长文本以省略号展示，横向滚动与列宽拖动更流畅

## What's Changed

### AI 与连接上下文

- AI 确认执行写操作统一复用 SQL 执行器的上下文切换逻辑
- PostgreSQL、高斯数据库会先切换目标物理库，再应用当前模式；达梦、Oracle、ClickHouse 也会正确继承当前上下文
- 扩展 JDBC 断线识别，预览、DDL、AI 等请求遇到已关闭连接时会自动重连并重试一次

### 高斯数据库

- 创建和删除数据库使用 JDBC 原生自动提交，避免被高斯拒绝为事务块中的语句
- SQL 编辑器和 SQL 文件执行 `CREATE/DROP DATABASE` 时同样使用自动提交；与普通事务 SQL 混合时会明确提示，避免部分执行

### 查询与界面

- 表预览和查询结果统一保持默认列宽，超长内容不再自动撑宽表格
- 修复列宽拖动时表格总宽度不同步导致横向滚动条跳动的问题
- 优化新建查询、SQL 格式化和执行按钮的组合样式，以及表预览工具栏的禁用和保存状态反馈

## Notes

- 当前版本号：`v0.2.15`
- 支持从 `v0.2.14` 直接升级

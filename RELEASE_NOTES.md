# DataDjinn v0.1.11

## Highlights

- 新增 ClickHouse 支持，补齐连接测试、库表浏览、表结构查看、只读查询与 SQL 执行链路
- 新增可视化建表能力，支持通过结构化表单直接创建表/集合
- 增强表结构编辑能力，支持表注释、字段注释、唯一、自增、自增步长、最小值、最大值等属性
- 重构表设计器交互，字段主行更简洁，详细属性改为展开式配置

## What's Changed

- 新增 ClickHouse 数据库接入与相关元数据、导出、查询支持
- 新增统一建表入口，补齐 PostgreSQL、MongoDB、ClickHouse 等类型的结构创建链路
- 扩展表结构更新接口与 schema，支持更多字段属性与表注释编辑
- 修复编辑表时字段注释不回显的问题
- 修复达梦查询结果中 `CLOB`、`Text` 等大字段只显示对象引用的问题
- 优化开发环境启动流程，前端启动时可自动拉起后端
- 补强安装版在线更新状态识别，下载完成状态判断更准确
- 修复部分页面和设置弹框中的中文乱码问题

## Notes

- 本次版本重点围绕多数据库支持、表结构设计能力增强和开发体验优化展开。

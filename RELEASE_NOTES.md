# DataDjinn v0.1.13

## Highlights

- 优化左侧资源树在多连接、多节点场景下的展开、收起和滚动性能，明显降低卡顿和分段感
- 重构驱动管理界面，并抽象手动驱动型数据库的管理模型，为后续新增国产数据库驱动做准备
- 增强达梦连接与驱动选择稳定性，修复编辑连接和切换驱动时可能导致界面白屏的问题

## What's Changed

- 左侧资源树减少展开收起时的重排和渲染抖动，提升大数据量节点下的交互响应速度
- 新增对象分类节点右键“查看列表”，可直接查看当前库下表或视图的名称与注释列表
- 表格预览区域补强单元格、行、列选择交互，并优化滚动与分页切换体验
- AI 操作数据库时支持连接关闭后的自动重连，同时将执行轮次上限从 12 提升到 24
- 驱动管理改为“数据库类型导航 + 当前类型列表/表单”的后台式布局，更适合扩展多种手动驱动数据库
- 达梦驱动选择与历史字段回填逻辑增强，兼容旧连接数据并避免驱动切换时的异常渲染
- 元数据接口支持按需返回表注释，便于前端展示表/视图列表等只读目录信息

## Notes

- 当前版本号为 `v0.1.13`
- 本次发布包含前端与后端联动改动，发布前已完成前端 typecheck、前端构建和后端语法校验

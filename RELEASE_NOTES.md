# DataDjinn v0.2.8

## Highlights

- 新增 DataDjinn 原生连接导入导出，支持把当前所有连接安全迁移到另一台设备
- 导入导出支持完整保留密码、SSH 隧道、分组和排序结构，并通过口令加密保护敏感信息
- 资源区头部、新建连接入口和相关弹框样式继续优化，减少远程桌面场景下的视觉干扰

## What's Changed

- 新增 `.ddj` 连接包导出与导入能力，支持导出当前全部连接并在另一台设备通过口令解密恢复
- 导出内容现在覆盖连接密码、SSH 配置、分组层级和连接排序，便于完整迁移工作环境
- 修复跨设备导入时“密码不对或文件损坏”的兼容性问题，补齐口令 Unicode 归一化、BOM/空白清理和 Base64 兼容处理
- 优化资源区导入/导出双按钮、新建连接下拉和空白区创建入口的视觉样式，并修复亮色模式与口令输入框双边框问题
- 正式回归基线补强连接导入导出、连接创建/编辑和表格搜索等通用场景，发版前通用 smoke 用例已重新跑通
- Windows 安装包发布者信息同步更新为 `vhukze`

## Notes

- 当前版本号：`v0.2.8`
- 当前 GitHub Release 工作流仅构建并发布 Windows 安装包

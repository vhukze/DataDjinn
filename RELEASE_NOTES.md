# DataDjinn v0.3.9

## What's Changed

### 连接树与连接

- 连接树搜索可定位已加载但折叠在库、模式或表分类下的匹配节点；上下切换会自动展开父路径并滚动到目标。
- 未保存密码的连接改为“保存并重新连接”；确认后立即关闭输入框，并在后台保存密码和执行重连。
- 修复重连期间数据库树与数据库勾选弹框使用不同选择状态导致的显示数量不一致。
- MySQL、PostgreSQL、ClickHouse、Oracle 等连接使用短建连超时；界面打开和测试连接最多等待约 10 秒。

## Verification Scope

- Git 表数据版本管理仍在验证中，不作为本版已完成能力发布。
- 连接分组覆盖安装后的恢复问题仍待用户实际安装验证，不作为本版修复项承诺。

## Notes

- 当前版本号：`v0.3.9`
- 支持从 `v0.3.8` 直接升级。

**Full Changelog**: https://github.com/vhukze/DataDjinn/compare/v0.3.8...v0.3.9

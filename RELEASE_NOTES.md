# DataDjinn v0.2.9

## Highlights

- 新增 SSH 隧道配置与测试能力，连接新建/编辑弹框改为更适合复杂配置的横向布局
- 连接导入导出继续增强，现已支持加密 `.ddj` 全量迁移，以及 DBeaver `data-sources.json` 导入
- 修复一批资源树、右键菜单、表格刷新、搜索、选择和编辑相关的高频交互问题，工作区稳定性进一步提升

## What's Changed

- 新增 SSH 隧道配置项，支持主机、端口、用户名、密码/私钥认证，并提供单独的 SSH 连通性测试按钮
- 连接编辑弹框现在会正确回显已保存的 SSH 字段，连接基础信息与 SSH 配置都可继续编辑
- 新增 DataDjinn 连接包导出/导入，`.ddj` 文件可完整保留连接参数、密码、SSH 隧道、分组和排序结构，并通过口令加密保护敏感信息
- 导入能力新增 DBeaver 支持，可解析 `data-sources.json` 中的连接与分组结构
- 优化资源树头部导入/导出入口、新建连接下拉和相关弹框样式，改善窄宽度与远程桌面场景下的显示效果
- 修复资源树右键菜单定位、连接关闭后勾选入口残留、连接切换延迟、树节点勾选弹框打开时机与位置异常等问题
- 修复表格分页栏位置、搜索高亮、刷新恢复原值、编辑失焦、右键视图、单元格选择态和新增行自动滚动等一批工作区交互问题
- 回归测试补强了连接迁移、树右键定位、连接选择、表格编辑与刷新等正式 smoke 场景，本次发布前已全部验证通过

## Notes

- 当前版本号：`v0.2.9`
- 当前 GitHub Release 工作流仅构建并发布 Windows 安装包

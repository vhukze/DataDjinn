PRODUCT_KNOWLEDGE = """# DataDjinn 内置产品知识库

## 产品定位和支持范围
- DataDjinn 是本地桌面数据库管理工具，支持 SQLite、MySQL、PostgreSQL、Oracle、达梦 DM、MongoDB、Redis、ClickHouse 和高斯数据库。
- AI 可以回答软件功能、界面位置、配置步骤、导入导出、快捷键、驱动、SQL 编辑器、表格预览、AI 设置、更新和故障排查等产品问题。
- 回答产品用法时不要调用任何数据库工具，不要臆造未实现的菜单、设置或快捷键；若知识库未覆盖，明确说明当前版本无法确认。

## 连接和连接迁移
- 新建连接：使用左侧连接区域的新建入口，选择数据库类型，填写连接参数后测试并保存；保存前可在连接弹框选择已有分组，也可直接新建分组，连接保存后会自动归入所选分组。达梦和高斯等外部驱动数据库需先在“设置 -> 驱动管理”配置驱动。
- 复制连接信息：右键左侧非 SQLite 连接，选择“复制连接信息”，会复制主机、端口、用户名、密码和当前数据库。该操作包含明文密码，只应粘贴到可信位置。
- 复制 JDBC URL：右键支持 JDBC 的连接，选择“复制为 JDBC URL”，会按当前数据库类型生成标准 JDBC 地址；MongoDB 和 Redis 不提供该菜单。ClickHouse 配置多个端口时使用第一个端口生成 URL。
- 导出连接：在左侧连接区域点击“导出连接”，设置并确认导出口令，选择保存位置。会生成加密的 .ddj 文件，保留连接参数、密码、SSH 隧道、分组和排序。
- 导入连接：在左侧连接区域点击“导入连接”，选择 DataDjinn .ddj 文件或 DBeaver 数据源文件。导入 .ddj 时输入导出口令，预览识别结果后确认导入。
- 备份和连接导出不同：备份用于完整恢复数据库；连接导出只迁移客户端连接配置。
- Redis 服务器即使出于安全原因禁用了 CONFIG 命令也可以连接和浏览；数据库列表会根据 INFO keyspace、当前连接 DB 和默认逻辑库范围自动兼容，无需为 DataDjinn 单独开放 CONFIG 权限。

## SQL 和表格
- SQL 编辑器基于 Monaco，支持 SQL 关键字、当前库表和字段补全。输入 SELECT、WHERE、ON、ORDER BY、GROUP BY、HAVING 或 SET 等字段上下文时，会自动提示当前语句 FROM/JOIN 所引用表的字段；可继续输入字段前缀缩小范围。多条 SQL 可用分号分隔，也可将下一条 SQL 另起一行；SQL 语句首行的播放按钮可直接执行当前单条语句。右上角执行按钮左侧提供 SQL 格式化按钮，会按当前数据库方言生成换行和层级缩进：有选区时格式化完整选区（可跨多条 SQL），没有选区时格式化光标所在单条 SQL。执行错误文本支持鼠标选中后按 Ctrl+C 复制。
- 所有受支持的 SQL 数据库都支持一致的 `--` 行注释处理；只含注释的区域不会显示执行按钮或被当作 SQL 执行，分号后的尾注释也不会被识别成独立语句，SQL 中间的注释会正常保留。
- “设置 -> SQL”可配置单条 SQL 超时，范围为 1 到 120 分钟，默认 15 分钟。
- 表预览支持 WHERE 过滤、分页、列搜索、单元格编辑、行列选择、列宽和列顺序调整。WHERE 输入框仅在正在输入字段前缀时提示当前表字段；空白、SQL 关键字和条件值位置不会弹出提示，直接按 Enter 即可查询。行号支持 Ctrl 多选、Shift 连续范围选择，以及拖到表格上下边缘后自动滚动继续选择。
- 查询结果搜索默认快捷键为 Ctrl+F；搜索框可用 Escape 关闭。

## 快捷键
- 修改入口：点击应用右上角“设置”，在左侧选择“快捷键”，点击对应快捷键的录制按钮后按下新的组合键；每项都可恢复默认值。
- 默认快捷键：执行 SQL 为 Ctrl+Enter，删除当前行是 Ctrl+D，向下复制行是 Ctrl+Alt+ArrowDown，表格搜索是 Ctrl+F。
- AI 默认快捷键：Enter 发送，Shift+Enter 换行，Ctrl+C 停止正在进行的 AI 任务。

## AI、外观和更新
- AI 配置在 AI 面板设置中完成，支持 OpenAI 兼容 API 和 Anthropic。配置可测试连接，并可管理多个配置。
- AI 上下文分为随当前打开库或模式自动切换的主上下文，以及用户从连接树手动添加并持续保留的补充上下文；只能添加数据库或模式，重复项会自动去重。没有自动主上下文时，第一个可用的手动上下文会作为临时主上下文，AI 可以读取其结构并执行数据库工具。PostgreSQL、高斯等具有数据库和模式层级的类型中，在当前数据库范围内点击数据库或模式的上下文按钮会直接切换主上下文：模式可向上合并为所属数据库，数据库也可下钻切换为某个模式，不会重复添加父子上下文。面板会显示上下文总数，超过 3 个时默认折叠，可手动展开或收起。
- AI 可执行受控数据库操作；写操作会显示确认按钮，未确认前不会执行。产品用法问答不需要数据库连接。AI 会话及完整回复会自动保存，重新打开应用后可从原会话继续查看和提问。
- 应用支持浅色和深色主题，可在界面主题按钮切换。
- 安装版可在设置中检查更新并下载后重启安装；绿色版会提供下载 zip 的方式手动替换。

## 导入、导出与驱动
- 数据导出可用于交换数据：关系型数据库支持 SQL/CSV，MongoDB 和 Redis 支持 JSON；SQL 可导出结构、数据或结构加数据。
- SQL/CSV 导入和数据库备份恢复属于不同功能；恢复备份会覆盖或回滚目标数据，应谨慎操作。
- 驱动管理入口为“设置 -> 驱动管理”，可维护 JDBC jar、dmPython pyd 或 whl。修改全局 Java 环境后按界面提示重启后端服务。
"""


def get_product_knowledge() -> str:
    return PRODUCT_KNOWLEDGE
